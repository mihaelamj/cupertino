# Scripts

Repository scripts that are part of the maintainer workflow.

Script documentation follows the same folder-first convention as command
documentation: folder names encode the script path, and nested `argument (<>)`
or `option (--)` directories describe its surface.

| Script | Description |
|--------|-------------|
| [eval/release-corpus-smoke](eval/release-corpus-smoke/) | Run the on-demand release-corpus smoke gate for a repo-built `cupertino` binary |
