# --help

Print usage for the release-corpus smoke script and exit without building or
running probes.

```bash
scripts/eval/release-corpus-smoke.sh --help
```

This is script-local help. It is not listed in `cupertino --help` because the
smoke gate is a repository maintainer script, not a `cupertino` CLI subcommand.
