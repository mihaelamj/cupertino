# --source

Source to browse.

## Synopsis

```bash
cupertino list-documents --source apple-docs --framework swiftui
```

## Description

Defaults to `apple-docs`. The command opens the per-source database declared by the source provider. In the current desktop contract, document listing is enabled for `apple-docs`.

## Default

`apple-docs`
