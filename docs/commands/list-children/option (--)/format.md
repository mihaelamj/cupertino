# --format

Select the output format.

## Usage

```bash
cupertino list-children apple-docs://swiftui --format json
```

## Values

- `json` (default)
- `text`
- `markdown`
- `md` (alias for `markdown`)

Use `json` for desktop clients and scripts.
