import Testing
@testable import DocsSearchToolProvider

// MARK: - Tool Provider Tests

@Test("Tool provider initializes correctly")
func testToolProviderInitialization() {
    // This is a basic smoke test to ensure the test target exists
    // Actual testing would require setting up a search index
    #expect(Bool(true))
}
