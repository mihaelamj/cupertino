// The `Search` + `Sample` namespace roots and the read value types /
// protocols are owned by CupertinoDataKit (single source of truth). This
// re-export makes them visible to every consumer of SearchModels, which
// extends those namespaces with cupertino's producer-side types.
@_exported import CupertinoDataKit
