import Foundation

// MARK: - Home View Stats

struct HomeStats {
    let totalPackages: Int
    let selectedPackages: Int
    let downloadedPackages: Int
    let artifactCount: Int
    let archiveGuideCount: Int
    let totalSize: Int64
}
